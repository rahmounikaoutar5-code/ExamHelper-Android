# Architecture

This project is organized as follows:

- src/model  
  Core domain objects (Subject, Chapter)

- src/storage  
  Persistence layer (XML, later database)

The goal is to keep the core logic independent from any UI.
